export interface SkillInfo {
  name: string;
  description: string;
  version: string;
  path: string;
}

export interface SkillMetadata {
  name: string;
  description: string;
  version?: string;
}
